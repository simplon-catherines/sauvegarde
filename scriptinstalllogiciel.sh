# decompression de l'archive

cd ~/

tar -xvzf ~/Bureau/backup.tar.gz Bureau/ .mozilla/

#installation des logiciels

#(capture d'ecran)
sudo apt-get install shutter

#(activer pavé numérique via la touche maj activée)
sudo apt-get install gnome-tweak-tool

#(editeur de texte atom)
sudo add-apt-repository ppa:webupd8team/atom
sudo apt-get update
sudo apt-get install atom

#presse papier parcellite
sudo apt-get install parcellite

#installation du logiciel git
sudo apt-get install git
git-config --global user.name "simplon-catherines"
git-config --global user.email sktystrugala@gmail.com
